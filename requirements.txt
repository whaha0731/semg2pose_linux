# ================================================================
# semg2pose 项目 Linux 依赖文件
# 用于从表面肌电图(sEMG)信号预测手部姿态
# ================================================================

# ----------------------------------------------------------------
# 核心深度学习框架
# ----------------------------------------------------------------
# PyTorch - 深度学习核心框架
# 注意: 根据您的系统选择合适的版本
# CPU版本: torch>=2.0.0
# CUDA 11.8: torch>=2.0.0+cu118
# CUDA 12.1: torch>=2.0.0+cu121
torch>=2.0.0
torchvision>=0.15.0

# ----------------------------------------------------------------
# 数值计算和科学计算
# ----------------------------------------------------------------
# NumPy - 数值计算基础库
numpy>=1.24.0

# SciPy - 科学计算库(用于信号处理)
# 用途: signal.decimate(降采样), signal.hilbert(包络提取)
scipy>=1.10.0

# ----------------------------------------------------------------
# 数据处理
# ----------------------------------------------------------------
# h5py - HDF5文件读写
# 用途: 读取 emg2pose_dataset_mini 中的 .hdf5 数据文件
h5py>=3.8.0

# ----------------------------------------------------------------
# 可选依赖(推荐安装)
# ----------------------------------------------------------------
# tqdm - 进度条显示(推荐用于训练过程可视化)
tqdm>=4.65.0

# matplotlib - 数据可视化(可选,用于结果分析)
matplotlib>=3.7.0

# pandas - 数据处理(可选,用于日志记录)
pandas>=2.0.0

# ----------------------------------------------------------------
# 开发和调试工具(可选)
# ----------------------------------------------------------------
# tensorboard - TensorBoard可视化(可选)
# tensorboard>=2.13.0

# pytest - 单元测试(开发时可选)
# pytest>=7.3.0

# ================================================================
# 安装说明
# ================================================================
#
# 1. 创建虚拟环境(推荐):
#    python3 -m venv venv
#    source venv/bin/activate
#
# 2. 安装依赖:
#    pip install -r requirements.txt
#
# 3. 如果需要GPU支持,请访问 PyTorch 官网选择合适的安装命令:
#    https://pytorch.org/get-started/locally/
#
#    示例(CUDA 11.8):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
#    示例(CUDA 12.1):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 4. 验证安装:
#    python -c "import torch; print(f'PyTorch版本: {torch.__version__}')"
#    python -c "import torch; print(f'CUDA可用: {torch.cuda.is_available()}')"
#
# ================================================================
# 系统要求
# ================================================================
#
# - Python: 3.8 或更高版本
# - 操作系统: Linux (Ubuntu 20.04+, CentOS 7+, etc.)
# - 内存: 至少 8GB RAM
# - 存储: 至少 2GB 可用空间
# - GPU(可选): NVIDIA GPU with CUDA 11.8+ (推荐用于训练)
#
# ================================================================
